spring:
  datasource:
    url: jdbc:mariadb://localhost:13306/realestate
    username: realestate
    password: realestate
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: localhost
      port: 6379
